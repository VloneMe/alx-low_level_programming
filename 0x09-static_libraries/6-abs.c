#include "main.h"

/**
 * _abs - the abs function
 *
 * @r: an interger value
 *
 * Return: absolute value of n number
 *
 */

int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);

}
