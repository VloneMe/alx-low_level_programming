#include <stdio.h>

/**
 * main - main entry of a program.
 *
 * Return: Always 0 (success)
 *
 */

int main(void)
{
	char A;

	for (A = 'a'; A <= 'z'; A++)
		putchar(A);
		putchar("%\n");

	return (0);
}
